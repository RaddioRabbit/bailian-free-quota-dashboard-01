# ──────────────────────────────────────────────────────────────
# 方式1（推荐）：阿里云账号登录（Playwright 自动抓取真实额度）
# ──────────────────────────────────────────────────────────────
# 无需配置任何环境变量。
# 在 Dashboard 页面点击"登录阿里云账号"按钮，会弹出浏览器让你登录。
# 登录后 session 自动保存到 .session.json（已加入 .gitignore）。

# ──────────────────────────────────────────────────────────────
# 方式2（备选）：API Key（仅用于模型调用，额度查询可能不可用）
# ──────────────────────────────────────────────────────────────
# DASHSCOPE_API_KEY=sk-your-api-key-here

# 可选: 缓存配置（毫秒，默认 5 分钟）
# CACHE_TTL=300000
